import React, {FC} from 'react'

interface Props {
    className?: string;
    title: string;
}

export const CheckboxFiltersGroups: FC<Props> = ({className}) => {
return (
    <div className={className}>

    </div>
)
}